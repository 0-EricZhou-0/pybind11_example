# Example `pybind11` Module

## Installation

Refer to [cmake/README.md](cmake/README.md).

Rich interface will only be available on debug build and when overriding the build system setup with `cmake -DENABLE_RICH_INTERFACE=ON ..` in build folder.

## Note
